apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-api-gateway
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-api-gateway.sm-platform.example.com
      secretName: sm-api-gateway-tls
  rules:
    - host: sm-api-gateway.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-api-gateway
                port:
                  number: 8300
